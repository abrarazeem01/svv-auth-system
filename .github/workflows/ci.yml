  name: Debug Validation Folder
  run: |
    echo "Inside project-root:"
    ls project-root

    echo "Inside Validation folder:"
    ls project-root/Validation || echo "Validation folder not found"

    echo "Detailed listing:"
    ls -l project-root/Validation || true
